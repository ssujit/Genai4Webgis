# Documentation

- `architecture/`: diagrams used in the thesis (layers, pipeline, validation/fallback).
- `screenshots/`: UI screenshots for the README or defense slides.

Tip: keep images small and compressed so cloning stays fast.
